---
import AppIcon from './app-icon.astro'
import AppDlButton from './app-dl-button.astro'
import {Platform} from "../content/config";
---

<div class="hero w-full lg:h-lvh  h-auto lg:pt-10 pt-40 flex items-center justify-center px-2">
  <div class="hero-main flex flex-col gap-10 justify-between lg:w-5/6 w-full items-center lg:items-start">
    <div class="flex flex-col justify-around">
      <h2 class="text-5xl leading-8">マリオカート8DXを<br>もっと楽しもう！</h2>
      <p class="mt-8">スマホで手軽に記録を入力できる、<br>
        マリオカート8DX好きのための戦績管理アプリ</p>
    </div>
    <div class="card backdrop-blur-sm bg-white/30 lg:p-10 p-2 flex flex-col gap-6 items-center justify-around">
      <div class="flex justify-start lg:items-center items-start gap-4 w-full">
        <AppIcon class="shrink-0 grow-0 hidden lg:block" size="120"/>
        <AppIcon class="shrink-0 grow-0 lg:hidden block" size="80"/>
        <div class="my-shadow">
          <h1 class="lg:text-3xl text-xl">TurboMK</h1>
          <p class="text-sm">for マリオカート</p>
          <p class="text-sm font-normal">無料でダウンロード！</p>
        </div>
      </div>
      <div class="flex justify-between flex-wrap gap-2 w-full">
        <AppDlButton class="lg:h-16 h-12" platform={Platform.iOS}/>
        <AppDlButton class="lg:h-16 h-12" platform={Platform.Android}/>
      </div>
    </div>
  </div>
</div>

<style lang="sass">
  .hero
    min-height: 100lvh
    
    &::after
      content: ""
      position: absolute
      top: 0
      left: 0
      width: 100%
      height: 100%
      z-index: 0
      background-image: url("/src/assets/hero_pc.webp")
      /*background-size: auto 120vh*/
      background-size: 120vw auto
      background-repeat: no-repeat
      /*background-position-x: -20vw*/
      /*background-position-y: -10vh*/
      background-position-x: center
      background-position-y: center

      @media (max-width: 960px)
        $sp-width: 160vw
        $sp-height: 120vh
        background-image: url("/src/assets/hero_sp.webp")
        /*background-size: $sp-width auto*/
        background-size: auto $sp-height
        opacity: 0.4
        /*background-position-x: calc(-1 * ($sp-width - 100vw) / 2)*/
        background-position-x: center
        background-position-y: center


  .hero-main
    z-index: 1
    max-width: 90vw


  .card
    width: 480px
    border-radius: 16px

    @media (max-width: 960px)
      width: 90vw
      max-width: 480px

</style>
